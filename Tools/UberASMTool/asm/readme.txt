This folder contains all .asm files inserted by Asar.

base folder contains the base ASM files which the tools edits and
moves them to the work folder. You don't need to edit any of the
files unless you are a programmer.

work folder contains the edited base ASM files which are inserted
on the ROM. They're generated by the tool during at runtime.