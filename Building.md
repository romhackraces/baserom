## Building Baserom

If you're using the baserom from source, set up is not much different than the distributed version, except for the maintenance of the retry-system submodule.

### Windows

1. Run `build.ps1` this will update the retry-system submodule and install its files, and preserve the baserom's retry config.

### Linux/Mac

1. Run `build.sh` which does the same as above.